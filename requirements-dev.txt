# Dev / CI dependencies shared across all backend services.
# Service runtime deps live in backend/<service>/requirements.txt.

ruff==0.6.9
black==24.10.0
isort==5.13.2

pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-mock==3.14.0

# Runtime libs used by unit tests across services (typically already present
# via each service's requirements.txt, but pinned here for CI reproducibility).
pyjwt>=2.8.0
cryptography>=42.0.0
structlog>=24.1.0
sqlalchemy>=2.0.23
python-dotenv>=1.0.0
grpcio==1.78.0
grpcio-tools==1.78.0
protobuf>=6.31.1
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
