apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: memegram
  labels:
    app: user-service
    app.kubernetes.io/part-of: memegram
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
        app.kubernetes.io/part-of: memegram
    spec:
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command:
            - sh
            - -c
            - "until nc -z user-postgres 5432; do echo waiting for postgres; sleep 2; done"
      containers:
        - name: user-service
          image: memegram/user-service:latest
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - "alembic upgrade head && python -m app.main"
          ports:
            - containerPort: 50052
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: POSTGRES_PASSWORD
            - name: DATABASE_URL
              value: "postgresql+asyncpg://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@user-postgres:5432/user_db"
            - name: GRPC_PORT
              value: "50052"
            - name: REDIS_URL
              value: "redis://user-redis:6379/0"
            - name: ENVIRONMENT
              value: development
            - name: SERVICE_VERSION
              value: "1.0.0"
          readinessProbe:
            tcpSocket:
              port: 50052
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: memegram
  labels:
    app: user-service
    app.kubernetes.io/part-of: memegram
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - port: 50052
      targetPort: 50052
