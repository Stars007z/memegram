apiVersion: v1
kind: Service
metadata:
  name: messaging-postgres
  namespace: memegram
  labels:
    app: messaging-postgres
    app.kubernetes.io/part-of: memegram
spec:
  clusterIP: None
  selector:
    app: messaging-postgres
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: messaging-postgres
  namespace: memegram
  labels:
    app: messaging-postgres
    app.kubernetes.io/part-of: memegram
spec:
  serviceName: messaging-postgres
  replicas: 1
  selector:
    matchLabels:
      app: messaging-postgres
  template:
    metadata:
      labels:
        app: messaging-postgres
        app.kubernetes.io/part-of: memegram
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              value: messaging_db
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - memegram
                - -d
                - messaging_db
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
